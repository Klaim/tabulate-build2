cxx.std = latest

using cxx

hxx{*}: extension = hpp
ixx{*}: extension = ipp
txx{*}: extension = tpp
cxx{*}: extension = cpp

# The test target for cross-testing (running tests under Wine, etc).
#
test.target = $cxx.target


upstream_dir = $src_root/upstream
include_dir = $upstream_dir/include
samples_dir = $upstream_dir/samples

